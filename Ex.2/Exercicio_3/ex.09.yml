info:
  name: ex.09
  type: http

http:
  method: PATCH
  url: https://jsonplaceholder.typicode.com/posts/1?"title"="Novo título"
  params:
    - name: '"title"'
      value: '"Novo título"'
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
